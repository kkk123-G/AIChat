# Set VITE_API_BASE_URL to the deployed API domain when frontend and backend use different origins.
# Leave it empty when production reverse proxy serves the API under the same /api path.
VITE_API_BASE_URL=
