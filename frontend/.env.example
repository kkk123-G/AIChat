# Development only: Vite forwards /api to this backend target.
VITE_BACKEND_PROXY_TARGET=http://127.0.0.1:8080

# Production API base URL. Leave empty for same-origin /api reverse proxy deployment.
VITE_API_BASE_URL=
